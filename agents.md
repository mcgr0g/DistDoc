# AGENTS.md — DistDoc

## Язык
- Все создаваемые артефакты (документы, заметки, комментарии) — русский, как существующая документация.

## Стек
- Java 25 (toolchain в build.gradle), Gradle wrapper 9.3.0, mise (tools: gradle 9.6.0, java temurin-25, jaq), Trino 481 (trinoVersion), distdocVersion 1.1.0 — оба в gradle.properties.
- Сборка только через mise run … (см. agents_bootstrap.md); прямой ./gradlew — по необходимости.

## Верификация сборки
- Перед сдачей любых изменений в build.gradle, sourceSets, dependencies — ОБЯЗАТЕЛЬНО запуск `./gradlew clean build`.
- Причина: `./gradlew check` возвращает `UP-TO-DATE` для уже скопированных ресурсов и не выявляет ошибки дублирования/конфликта задач. Только `clean build` гарантирует проверку с нуля.
- CI всегда запускается на чистом checkout — локальная верификация должна эмулировать это.

## Ключевые запреты
1. НЕ менять регистр JSON-ключей и служебные BSON-ключи ($oid, $date, $numberLong, @type, @version) — ни при парсинге, ни в путях, ни в SQL.
2. НЕ строить DOM-дерево JSON: только однонаправленный стриминг, O(1) памяти на строку (паттерн 1 из docs/patterns/plugin.md).
3. НЕ глушить исключения и предупреждения: fault tolerance (паттерн 5) — осознанные перехваты только в критических фазах разбора/десериализации.
4. НЕ править docs/contracts/rx-data-contract.md без синхронизации с local/generator/ (контракт общий для двух компонентов).

## Протокол памяти
- local/steps.md — внешняя долговременная память на случай внешних или локальных сбоев. Перед каждым новым шагом перечитывать целиком; после каждого шага дописывать запись итога.
- Исследования и оценки без реализации (мнение о возможности) оформлять заметкой local/research/<тема>.md; реализация по ним — отдельной задачей.

## Правило синхронизации документации
- Изменил контракт схемы/фикстур → обнови соответствующий документ: docs/contracts/rx-data-contract.md, docs/testing/fixtures.md; для значимых решений — новый ADR в docs/adr/.

## Структура документации
- Карта проекта: agents_bootstrap.md (читать при входе в незнакомую область).
- Частицы плагина: docs/ (contracts/, adr/, patterns/, testing/, guides/).
- Документация генератора: local/generator/ (вне git, стиль тот же).
- local/ — вне git (служебный каталог проекта).
